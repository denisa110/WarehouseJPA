CREATE TABLE bags (ID  SERIAL NOT NULL, COLOR VARCHAR(255), SIZE INTEGER, WEIGHT FLOAT, person INTEGER, PRIMARY KEY (ID))
CREATE TABLE countries (ID  SERIAL NOT NULL, COUNTYNAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE deposits (ID  SERIAL NOT NULL, AVAILABLEBAG INTEGER, NAMEDEPO VARCHAR(255), depositList INTEGER, PRIMARY KEY (ID))
CREATE TABLE persons (ID  SERIAL NOT NULL, BIRTHDATE DATE, COUNTRY VARCHAR(255), FIRSTNAME VARCHAR(255), LASTNAME VARCHAR(255), PRIMARY KEY (ID))
ALTER TABLE bags ADD CONSTRAINT FK_bags_person FOREIGN KEY (person) REFERENCES persons (ID)
ALTER TABLE deposits ADD CONSTRAINT FK_deposits_depositList FOREIGN KEY (depositList) REFERENCES countries (ID)
